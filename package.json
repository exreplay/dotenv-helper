{
  "name": "set-dot-env",
  "version": "0.0.6",
  "description": "A helper for environment files",
  "bin": "./bin/set-dot-env.mjs",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "bin",
    "dist"
  ],
  "scripts": {
    "prepack": "unbuild",
    "build": "unbuild",
    "test": "vitest"
  },
  "devDependencies": {
    "@averjs/eslint-config": "^3.1.0",
    "@types/node": "16",
    "@types/prompts": "^2.4.1",
    "standard-version": "^9.5.0",
    "typescript": "^4.8.4",
    "unbuild": "^0.8.11",
    "vitest": "^0.23.4"
  },
  "dependencies": {
    "globby": "^13.1.2",
    "prompts": "^2.4.2"
  }
}
